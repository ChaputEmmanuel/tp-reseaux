cat << EOF > /etc/bgpd-as3r1.conf
hostname bgpd
password zebra
log stdout
router bgp 3
 bgp router-id 192.168.0.4
 neighbor 10.0.3.1 remote-as 1
 neighbor 10.0.5.2 remote-as 4
 network 172.23.0.0/16
EOF
bgpd -d -z /dev/null -f /etc/bgpd-as3r1.conf -i /tmp/bgpd-as3r1.pid
