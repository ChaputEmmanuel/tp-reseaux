cat << EOF > /etc/bgpd-as2r1.conf
hostname bgpd
password zebra
log stdout
router bgp 2
 bgp router-id 192.168.0.3
 neighbor 10.0.2.1 remote-as 1
 neighbor 10.0.4.2 remote-as 4
 network 172.22.0.0/16
EOF
bgpd -d -z /dev/null -f /etc/bgpd-as2r1.conf -i /tmp/bgpd-as2r1.pid
